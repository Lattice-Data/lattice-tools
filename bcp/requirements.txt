# Core dependencies for CRISPR guide processing pipeline
pandas>=1.3.0
requests>=2.28.0
pathlib2>=2.3.0; python_version < "3.4"

# Bioframe dependency for gene annotation
bioframe>=0.4.0

# Dependencies for qa_mods (used by test_manifest_loading and other bcp tooling)
boto3>=1.20.0
beautifulsoup4>=4.9.0
fsspec>=2023.1.0
s3fs>=2023.1.0

# Dependency for chebi_gate: structure keys for the ChEBI submission gate.
# Not optional and not lazily imported. An InChIKey cannot be derived from a
# molfile without it, and a gate that skipped its structure checks when RDKit was
# absent would clear records on the strength of having checked nothing.
rdkit>=2024.3.1

# Dependencies for graph_db2
dash>=2.0.0
dash-cytoscape>=1.0.0
db2-flattener @ git+https://github.com/Lattice-Data/db2-flattener@v0.1.0

# Testing dependencies
pytest>=6.0.0
pytest-cov>=2.0.0
tqdm>=4.60.0

# Optional: For performance and data analysis
numpy>=1.20.0
openpyxl>=3.1.0
